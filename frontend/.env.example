# Local (npm run dev)
VITE_API_URL=http://localhost:5000

# Producción: ya está en .env.production → Render
# Si cambias de servidor, actualiza frontend/.env.production y redeploy en Vercel

# Panel admin (fallback si Render no tiene ADMIN_PASSWORD)
VITE_ADMIN_USER=admin
VITE_ADMIN_PASSWORD=admin
